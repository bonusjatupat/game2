import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)
import java.awt.Color;
/**
 * Write a description of class eatPeople here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class eatPeople extends ScoreBoard
{
    /**
     * Act - do whatever the eatPeople wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
    }    
    
    public eatPeople(){
        setImage(new GreenfootImage(""+MyWorld.eatPeople, 35, Color.white, null)); 
    }
}
