import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)
public class EatStuff extends ScoreBoard
{  
    public EatStuff(){ // default constructor
        setImage(new GreenfootImage(""+Game.eatStuff, 35, Color.WHITE,  new Color(0,0,0,0))); // set score of eat good stuffs
    }
}
