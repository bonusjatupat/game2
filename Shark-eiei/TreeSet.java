import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)
public class TreeSet extends Trees
{
    public TreeSet(){ // default constructor
        GreenfootImage treeSet = new GreenfootImage("treeSet.png"); // create variable to store image
        setImage(treeSet); // set it
    }
}
